plugins {
    id 'java'
}

group = 'com.example'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    // Note: this version is vulnerable (prior to 4.1.124.Final)
    implementation 'io.netty:netty-codec-http2:4.1.121.Final'
    
    // other dependencies ...
    // implementation 'io.netty:netty-handler:4.1.121.Final'
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
